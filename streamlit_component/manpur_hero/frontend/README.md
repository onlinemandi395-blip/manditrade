# Streamlit hero build target

Copy the built contents of `frontend/dist` into this folder's `dist/` directory when you want the
custom Streamlit component to render the React-based 3D hero scene.

Expected path:

```text
streamlit_component/manpur_hero/frontend/dist
```
